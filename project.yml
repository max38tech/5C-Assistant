name: Antigravity5C
options:
  bundleIdPrefix: com.antigravity
targets:
  Antigravity5C:
    type: application
    platform: iOS
    deploymentTarget: "10.3"
    sources:
      - App
    settings:
      base:
        INFOPLIST_FILE: App/Info.plist
        PRODUCT_BUNDLE_IDENTIFIER: com.antigravity.5c
        TARGETED_DEVICE_FAMILY: "1" # iPhone only
        SWIFT_VERSION: "5.0"
